spring:
  application:
    name: order-service
  profiles:
    active: dev
    group:
      # Config Server에서 application-topics.yml 설정 제공
      dev: dev, topics
  config:
    import: 'optional:configserver:'

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:18761/eureka}

server:
  port: 18085